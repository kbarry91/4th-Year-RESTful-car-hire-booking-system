package ie.gmit.sw.Consumer;

import java.util.List;
import ie.gmit.sw.Consumer.*;
import ie.gmit.sw.models.*;

/*
 *  This class was used to test functions during the development
 *  
 *  */
public class TestClass {
	/*
	 * public static void main(String[] args) { // Get a handle on the RestAccess
	 * object. // Call the getBookings() method in the access object. RestAccess
	 * access = new RestAccess(); List<Booking> bookingList = access.getBookings();
	 * for (Booking booking : bookingList) {
	 * System.out.println("ID of booking:::"+booking.getBookingId()); }
	 * 
	 * }
	 */

}
